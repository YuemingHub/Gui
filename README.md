# 回到自己

一个本地优先、安静克制的个人前端工具，用来识别真事、清理噪音、重建节奏。

它不是任务管理器，不是打卡系统，也不是仪表盘。它不会评分、排名、制造连续性焦虑，也没有账号、云端同步或后台数据库。

## 当前状态

这是一个可运行的 Next.js 前端 MVP，重点先放在稳定和可恢复：

- 可以正常打开
- 输入内容会自动保存在当前浏览器
- 刷新后不会丢失当前内容
- 支持导出本地 JSON 备份
- 支持导入此前导出的 JSON 备份
- 支持重置为初始状态
- 对损坏或不完整的本地数据有兜底归一化处理

## 产品结构

应用包含 7 个核心模块：

1. **回到自己**：写下今天真正要照看的事
2. **真戏工坊**：沉淀 1–3 件真实重要的事
3. **清场室**：识别占用你的人和事，并决定限制、降级或移除
4. **静音舱**：提前写下边界与回应方式
5. **三色节奏**：按绿色 / 黄色 / 红色状态安排推进、维护或保护
6. **半成品花园**：收纳草稿、想法与未成熟项目
7. **月度清场**：做无评分的月度整理

## 技术栈

- Next.js 16 App Router
- React 19
- TypeScript
- Tailwind CSS v4
- localStorage 本地持久化

## 运行方式

```bash
npm install
npm run dev
```

默认开发命令使用 Webpack 版本的 Next dev，以优先保证本地开发稳定性。

打开浏览器访问：

```text
http://localhost:3000
```

## 可用脚本

```bash
npm run dev        # 本地开发（webpack）
npm run dev:turbo  # 备用 turbo dev
npm run lint       # ESLint
npm run build      # 生产构建
npm run start      # 启动生产构建产物
```

## 数据与隐私

所有内容默认只保存在当前浏览器：

- 没有登录
- 没有服务端存储
- 没有远程同步
- 没有埋点或后台分析逻辑

这意味着：

- 清浏览器数据会导致本地内容丢失
- 若想长期保留，请主动使用“导出本地备份”
- 若想恢复，请导入此前导出的 JSON 文件

## 交互原则

这个项目刻意避免以下机制：

- 分数
- 排名
- 连续打卡
- 进度羞辱
- 夸张提醒
- 仪表盘式压迫感

界面与文案尽量保持温和、低刺激、反劫持。

## 目录概览

```text
app/
  hooks/useAppStorage.ts     # 本地存储、水合、导入导出、重置
  lib/defaults.ts            # 初始默认值
  lib/storage.ts             # 读取、保存、归一化、导入导出
  lib/types.ts               # 全局类型定义
  page.tsx                   # 顶层 section 路由与状态接线
components/
  layout/                    # 应用外壳、导航、欢迎引导、数据工具区
  sections/                  # 7 个核心模块
  ui/                        # 通用 UI 组件
```

## 相关文档

- `ARCHITECTURE.md`：数据流、状态结构、持久化设计
- `PRODUCT_NOTES.md`：产品原则、范围边界、后续建议
- `QA_CHECKLIST.md`：手动验证清单

## 已知边界

当前版本有意不包含：

- 后端
- 用户系统
- 云同步
- 多端协同
- AI 自动生成
- 通知系统
- 复杂统计面板

目标不是做大而全，而是先做一个干净、稳定、能长期迭代的本地优先前端基线。
